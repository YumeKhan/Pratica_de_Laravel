// src/components/Layout.js
import React from 'react';
import './Layout.css'; // Verifique se o caminho está correto

function Layout() {
    return (
        <div className="layout">
            {/* Conteúdo do Layout */}
        </div>
    );
}

export default Layout;
